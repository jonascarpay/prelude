[package]
name = "graphics"
version = "0.1.0"
edition = "2024"

[dependencies]
prelude = { path = "../prelude" }
winit = "0.30.13"

[dev-dependencies]
prelude = { path = "../prelude", features = ["proptest"] }
proptest = "1"
